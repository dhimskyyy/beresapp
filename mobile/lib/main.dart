import 'main_user.dart' as user_app;

/// Entry point default untuk aplikasi Beres (Pelanggan / Konsumen).
/// Untuk menjalankan aplikasi Mitra Tukang secara spesifik, jalankan:
/// flutter run -t lib/main_mitra.dart
void main() {
  user_app.main();
}
